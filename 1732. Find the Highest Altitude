class Solution {
    public int largestAltitude(int[] gain) {
        int alt=0;
        int max=0;
        for(int a=0;a<gain.length;a++){
           alt+=gain[a];
            max=Math.max(alt,max);
        }
        return max;
    }
}
